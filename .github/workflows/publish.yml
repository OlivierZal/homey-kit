# Calls the family's reusable publish pipeline (pack, attest the
# tarball's provenance, publish it to GitHub Packages). The `npm`
# environment and the `id-token: write` the attestation needs travel
# with the called job; this stub grants them and keeps the release
# trigger.
jobs:
  publish:
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write
    uses: OlivierZal/configs/.github/workflows/reusable-publish.yml@9656fb8009b6b4c344dbdda62c99b95ed64bfe8f # v5.0.0
name: Publish package to GitHub Packages
on:
  release:
    types: [published]
permissions: {}
